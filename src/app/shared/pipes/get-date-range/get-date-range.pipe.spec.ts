import { GetDateRangePipe } from './get-date-range.pipe';

describe('GetDateRangePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDateRangePipe();
    expect(pipe).toBeTruthy();
  });
});
